@model IEnumerable<passionProject_n01333782.Models.CarManufactures>
@{ 

    ViewBag.Title = "List";

 }
<br/>

<div class="recadd">
    @Html.ActionLink("Add a New Manufacturer", "Create")
</div>
<div>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>            
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
        <tr>
            <td>

                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>

                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.ActionLink("Delete ","Delete", new { Id = item.Name_id })|
                @Html.ActionLink("Edit ","Edit", new { Id= item.Name_id})|
                @Html.ActionLink("Detail","Detail",new { Id = item.Name_id })

            </td> 
        </tr>
        }
    </table>
</div>
